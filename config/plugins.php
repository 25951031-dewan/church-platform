<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Core Plugins
    |--------------------------------------------------------------------------
    | These plugins cannot be disabled. Listed here so PluginManager skips
    | the can_disable check for them.
    */
    'core' => ['Auth', 'Settings'],

    /*
    |--------------------------------------------------------------------------
    | Plugin Registry Path
    |--------------------------------------------------------------------------
    */
    'registry' => storage_path('app/plugins.json'),

    /*
    |--------------------------------------------------------------------------
    | Plugins Base Path
    |--------------------------------------------------------------------------
    */
    'path' => base_path('plugins'),
];
